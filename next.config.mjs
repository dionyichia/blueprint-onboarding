/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.britannica.com',
      'bucket-files.city-sightseeing.com',
      'eos.org',
    ],
  },
};

export default nextConfig;
